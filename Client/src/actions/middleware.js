export const setMiddleware = (middleware = "loading") => ({
    type: "SET_MIDDLEWARE",
    middleware
});
export const setHashtags = (middleware = "loading") => ({
    type: "SET_HASHTAGS",
    middleware
});
export const setConnects = (middleware = "loading") => ({
    type: "SET_CONNECTS",
    middleware
});